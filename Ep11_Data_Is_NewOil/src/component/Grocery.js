const Grocery = () => {
  return (
    <div className="text-lg font-bold m-5">
      Assume That this is a big component that have lot's of component in it. We
      use it for lazy loading, chunking, codespliting, bundling.
    </div>
  );
};

export default Grocery;
