{
  "name": "ep12_lets_build_ourstore",
  "version": "1.0.0",
  "description": "- It is a global store (big object), where we kept data in slices.\r         - And we can not add data directally to slices :-\r                 1. Dispatch action\r                 2. It call the function (Reducer).\r                 3. and function modify the slices.\r         Eg. Supose we have add item to cart, than we click on add button it dispatch action it call the reducer function and function update the slices (cart).\r         - We will use `Selector` to read the data. this finomina is known as subscribe the store.",
  "main": "App.js",
  "scripts": {
    "start": "npx parcel index.html",
    "build": "npx parcel build index.html",
    "test": "jest"
  },
  "keywords": [],
  "author": "Gaurav Trivedi",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.10.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.18.0"
  },
  "devDependencies": {
    "postcss": "^8.4.31",
    "process": "^0.11.10",
    "tailwindcss": "^3.3.5"
  }
}
